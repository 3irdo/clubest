---
import { supabase } from '../lib/supabase'

const { data, error } = await supabase
  .from('roles')
  .select('*')
---

<html>
  <body>
    <pre>
      {JSON.stringify({ data, error }, null, 2)}
    </pre>
  </body>
</html>